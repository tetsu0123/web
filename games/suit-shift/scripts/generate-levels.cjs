'use strict';
// Legacy entry point: never silently recreate the uncurated version-one set.
console.error('Use npm run generate: the strategy edition requires exact C++ breadth-first search and curation.');
process.exitCode=1;
